# Application
spring.application.name=rd_system_project
server.port=${PORT:8080}

## Supabase PostgreSQL (via Session Pooler for IPv4 compatibility on Render)
## This URL (aws-0...) is REQUIRED for Render to avoid "UnknownHostException"
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.habinfrlmdtbgacgvhqt
spring.datasource.password=Rahul216746
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pooling
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SSL Configuration
spring.datasource.hikari.data-source-properties.sslmode=require
spring.jpa.properties.hibernate.connection.ssl=true

# OpenAPI/Swagger
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Debug logging
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE